import 'package:flutter/material.dart';

class AdminCarsManagementPage extends StatelessWidget {
  const AdminCarsManagementPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Cars en cours...',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
