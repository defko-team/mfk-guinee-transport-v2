import 'package:flutter/material.dart';

class AdminTravelsManagementPage extends StatelessWidget {
  const AdminTravelsManagementPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'En cours...',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
