import 'package:flutter/material.dart';

class AdminDashboardManagementPage extends StatelessWidget {
  const AdminDashboardManagementPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'En cours...',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
