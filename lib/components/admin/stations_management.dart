import 'package:flutter/material.dart';
import '../../views/admin/stations_page.dart';

class AdminStationsManagementPage extends StatelessWidget {
  const AdminStationsManagementPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const StationsPage();
  }
}
